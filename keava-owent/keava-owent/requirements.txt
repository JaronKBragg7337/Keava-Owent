# Core (required to run the loop)
apscheduler>=3.10
networkx>=3.0
psutil>=5.9
PyYAML>=6.0

# Receipts (Ed25519 signing). Optional but recommended.
cryptography>=42.0

# Outbound email via SendGrid REST. Optional; local outbox works without it.
requests>=2.31

# GPU energy metering on NVIDIA hardware. Optional.
# pynvml>=11.5

# Dev / tests
pytest>=8.0
